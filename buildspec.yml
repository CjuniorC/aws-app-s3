version: 0.1

phases:
  install:
    commands:
      - aws s3 rm s3://angularapplicationcjunior --recursive
      - aws s3 cp index.html s3://angularapplicationcjunior --grants read=uri=http://acs.amazonaws.com/groups/global/AllUsers

artifacts:
  files:
    - '**/*'